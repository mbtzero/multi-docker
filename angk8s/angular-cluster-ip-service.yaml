apiVersion: v1
kind: Service
metadata:
  name: angular-cluster-ip-service
spec:
  type: NodePort
  selector:
    component: angular
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 31000
